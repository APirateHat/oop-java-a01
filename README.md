#Compiling:

##Task 1:

To compile task 1 open the terminal. Find and change into the task_01 directory.

Assuming you have Java installed you can in the terminal type `javac *.java` to compile all scripts.
Afterwards excecute the main java file with "java Main".